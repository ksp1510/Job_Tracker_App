"""
Pydantic models for request and response payloads used by the AI microservice.

This package includes `requests` and `responses` modules defining data structures for
all API endpoints.  Import specific classes from those modules, e.g.:

```
from app.models.requests import JobSearchRequest
from app.models.responses import JobSearchResponse
```
"""