"""
Configuration package for the AI microservice.

The `settings` module contains Pydantic settings that read from environment variables
and a `.env` file.  Use `from .settings import settings` to access configuration values.
"""